// computeTotal(items, discountPct, taxPct)
//   items: array of { price, qty }
//   discountPct, taxPct: percentages (e.g. 10 means 10%)
// Apply the discount to the subtotal, then tax on the discounted amount.
// Empty cart totals 0. Currently a stub.
function computeTotal(items, discountPct, taxPct) {
  return 0;
}
module.exports = computeTotal;
