// Merge overlapping intervals. Each interval is [start, end] with start <= end.
// Return the merged set, sorted by start. Currently a stub that does nothing.
function merge(intervals) {
  return intervals;
}
module.exports = merge;
