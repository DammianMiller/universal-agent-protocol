// parseCSV(text) -> array of rows (each an array of string fields).
// Must handle: quoted fields, commas inside quotes, escaped quotes ("" -> "),
// LF and CRLF line endings, and a trailing newline (no empty trailing row).
module.exports = function parseCSV(text) {
  return [];
};
