def parse_duration(s):
    # TODO: parse a duration string like '1h30m', '45s', '2d', '1h30m15s' into
    # total SECONDS. Units: d=86400, h=3600, m=60, s=1. Combined units sum.
    # MUST raise ValueError on an invalid string (e.g. '', 'abc', '10', '1x').
    return 0
