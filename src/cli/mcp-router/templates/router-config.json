{
  "$schema": "https://json.schemastore.org/mcp-schema.json",
  "mcpServers": {
    "router": {
      "command": "npx",
      "args": ["uap", "mcp-router", "start"],
      "description": "Unified MCP Router - routes all tool calls through single endpoint"
    }
  },
  "metadata": {
    "version": "7.0.2",
    "autoGenerated": true,
    "replaces": ["individual MCP servers"]
  }
}
